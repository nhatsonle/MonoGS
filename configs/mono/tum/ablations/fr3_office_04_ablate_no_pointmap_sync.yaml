inherit_from: "configs/mono/tum/ablations/fr3_office_04_dust3r_event_refresh.yaml"

# Ablation from config 04:
# Keep DUSt3R depth bootstrap/refresh, but disable synchronized pointmap scaling.
# The baseline-ratio fallback remains enabled.

Training:
  dust3r:
    scale:
      baseline_ratio: True
      pointmap_sync: False

