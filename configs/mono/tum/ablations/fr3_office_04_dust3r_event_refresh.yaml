inherit_from: "configs/mono/tum/ablations/fr3_office_00_monogs.yaml"

# Config 04 — DUSt3R single-view init + event-driven refresh.
# All shared, sequence-independent DUSt3R / lifecycle tuning lives in the
# "event_refresh" preset (utils/config_presets.py). Add only scene-specific
# overrides (e.g. Dataset path/calibration) below if needed.
preset: event_refresh
Training:
  mapping_itr_num: 150
  lifecycle:
    enabled: False